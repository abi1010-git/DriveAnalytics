import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{BarChart,Bar,LineChart,Line,XAxis,YAxis,Tooltip,CartesianGrid,ResponsiveContainer}from'recharts';import'./style.css';
const API=import.meta.env.VITE_API_BASE_URL||'http://127.0.0.1:8000';type Event={id:number;vehicle_id:number;timestamp:string;event_type:string;severity:string;software_version:string;route_id:string;description:string};type Summary={total_vehicles:number;total_safety_events:number;hard_braking_count:number;sensor_failure_count:number;events_by_severity:Record<string,number>;events_by_event_type:Record<string,number>;events_by_software_version:Record<string,number>};
const chart=(o:Record<string,number>)=>Object.entries(o).map(([name,value])=>({name,value}));
function App(){const[s,setS]=useState<Summary|null>(null),[events,setEvents]=useState<Event[]>([]),[selected,setSelected]=useState<Event|null>(null),[severity,setSeverity]=useState(''),[error,setError]=useState<string|null>(null);const load=()=>{setError(null);setS(null);Promise.all([fetch(API+'/api/metrics/summary').then(r=>{if(!r.ok)throw Error(`Summary request failed (${r.status})`);return r.json()}),fetch(API+'/api/events').then(r=>{if(!r.ok)throw Error(`Events request failed (${r.status})`);return r.json()})]).then(([a,b])=>{setS(a);setEvents(b)}).catch(e=>setError(e instanceof Error?e.message:'Unable to load analytics'));};useEffect(load,[]);const shown=events.filter(e=>!severity||e.severity===severity);return <main><header><div><span className="eyebrow">SAFEDRIVE / ENGINEERING ANALYTICS</span><h1>Safety operations console</h1><p>Simulated fleet telemetry and event intelligence.</p></div><span className="badge">● SYNTHETIC DATA</span></header>{error?<section><p>Unable to load analytics from <code>{API}</code>: {error}</p><button onClick={load}>Retry</button></section>:!s?<p>Loading analytics…</p>:<><section className="cards">{[['Fleet vehicles',s.total_vehicles],['Safety events',s.total_safety_events],['Critical events',s.events_by_severity.CRITICAL||0],['Hard braking',s.hard_braking_count],['Sensor failures',s.sensor_failure_count]].map(([n,v])=><article key={String(n)}><small>{n}</small><strong>{v}</strong><span>Current dataset</span></article>)}</section><section className="charts"><Panel title="Events by severity"><BarChart data={chart(s.events_by_severity)}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#f59e0b"/></BarChart></Panel><Panel title="Event type distribution"><BarChart data={chart(s.events_by_event_type)}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name" angle={-18} textAnchor="end" height={55}/><YAxis/><Tooltip/><Bar dataKey="value" fill="#38bdf8"/></BarChart></Panel><Panel title="Software versions"><BarChart data={chart(s.events_by_software_version)}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="value" fill="#a78bfa"/></BarChart></Panel></section><section className="investigate"><div className="section-head"><div><span className="eyebrow">EVENT INVESTIGATION</span><h2>Recent safety events</h2></div><select value={severity} onChange={e=>setSeverity(e.target.value)}><option value="">All severities</option>{['LOW','MEDIUM','HIGH','CRITICAL'].map(x=><option key={x}>{x}</option>)}</select></div><table><thead><tr><th>Timestamp</th><th>Vehicle</th><th>Event type</th><th>Severity</th><th>Version</th><th>Route</th></tr></thead><tbody>{shown.slice(0,100).map(e=><tr key={e.id} onClick={()=>setSelected(e)}><td>{new Date(e.timestamp).toLocaleString()}</td><td>SD-{String(e.vehicle_id).padStart(3,'0')}</td><td>{e.event_type}</td><td><span className={'severity '+e.severity.toLowerCase()}>{e.severity}</span></td><td>{e.software_version}</td><td>{e.route_id}</td></tr>)}</tbody></table></section></>}{selected&&<aside><button onClick={()=>setSelected(null)}>×</button><span className="eyebrow">EVENT {selected.id}</span><h2>{selected.event_type}</h2><p>{selected.description}</p><dl><dt>Severity</dt><dd>{selected.severity}</dd><dt>Vehicle</dt><dd>SD-{String(selected.vehicle_id).padStart(3,'0')}</dd><dt>Timestamp</dt><dd>{new Date(selected.timestamp).toLocaleString()}</dd><dt>Route / Version</dt><dd>{selected.route_id} / {selected.software_version}</dd></dl><div className="telemetry-note">Nearby telemetry is available through <code>/api/events/{selected.id}</code>.</div></aside>}</main>};function Panel(p:{title:string;children:any}){return <article className="panel"><h3>{p.title}</h3><ResponsiveContainer width="100%" height={220}>{p.children}</ResponsiveContainer></article>}createRoot(document.getElementById('root')!).render(<App/>);
